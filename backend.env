# VoIP Backend Configuration
# Copy this file to your backend directory as .env

# Server Configuration
PORT=8080
HOST=0.0.0.0

# JWT Configuration
JWT_SECRET=voip-secret-key-change-in-production-2024
JWT_EXPIRY_HOURS=24

# Database Configuration
DB_PATH=./voip.db

# Asterisk Configuration - FIXED IP ADDRESS
ASTERISK_HOST=172.20.10.5
ASTERISK_AMI_PORT=5038
ASTERISK_AMI_USERNAME=admin
ASTERISK_AMI_SECRET=amp111

# SIP Configuration
SIP_DOMAIN=172.20.10.5
SIP_PORT=8088

# Public host for frontend connections
PUBLIC_HOST=172.20.10.5

# Environment and service info
ENVIRONMENT=development
SERVICE_NAME=voip-backend

# CORS Configuration - Allow frontend dev servers
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://192.168.1.2:3000,http://localhost:3001,http://127.0.0.1:3001,http://192.168.1.2:3001

# Service discovery
DISCOVERY_MODE=auto
DEBUG=true

# Performance optimizations
AMI_TIMEOUT=10
HTTP_TIMEOUT=5
WEBSOCKET_TIMEOUT=30
